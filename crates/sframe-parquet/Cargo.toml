[package]
name = "sframe-parquet"
version = "0.1.0"
edition = "2021"

[dependencies]
sframe-types = { path = "../sframe-types" }
sframe-query = { path = "../sframe-query" }
arrow = "58"
parquet = { version = "58", features = ["arrow", "snap"] }
glob = "0.3"

[dev-dependencies]
tempfile = "3"
